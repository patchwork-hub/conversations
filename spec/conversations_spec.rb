# frozen_string_literal: true

RSpec.describe Conversations do
  it "has a version number" do
    expect(Conversations::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
